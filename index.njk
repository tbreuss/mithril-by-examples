---
layout: layouts/home.njk
eleventyNavigation:
  key: Home
  order: 1
---

{% set maxExamples = collections.examples.length | min(3) %}

<h1>Latest {% if maxExamples == 1 %}Example{% else %}{{ maxExamples }} Examples{% endif %}</h1>

{% set examplesList = collections.examples | head(-3) %}
{% set examplesListCounter = collections.examples | length %}
{% include "examples_list.njk" %}

<p>More examples can be found in <a href="{{ '/examples/' | url }}">the examples</a> section.</p>
